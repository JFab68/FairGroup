import { useQuery } from 'react-query'; import api from '../../services/api';
const MemberAdmin = () => { const { data: members, isLoading } = useQuery('members', () => api.get('/api/members').then(res => res.data)); if (isLoading) return <div>Loading...</div>; return (<div><h1>Member Management</h1><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Subcommittees</th><th>Actions</th></tr></thead><tbody>{members?.map(member => (<tr key={member.id}><td>{member.first_name} {member.last_name}</td><td>{member.email}</td><td>{member.role}</td><td>{member.status}</td><td>{member.subcommittees?.join(', ') || 'None'}</td><td><button className='btn-primary' style={{ padding: '0.25rem 0.5rem', fontSize: '0.875rem' }}>Edit</button></td></tr>))}</tbody></table></div>); };
export default MemberAdmin;
